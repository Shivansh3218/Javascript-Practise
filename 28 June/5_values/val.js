for(let i =1; i<=4;i++){// Here i defines number of rows 
    let empty =' ' 
    for (let j =1;j<=5;j++){// This defines how many columns are there 
        empty = empty+ j // this is adding j to make columns 
        // console.log(empty)    
        
        // 1 in first line tghen 1 2 in second 1 2 3 in third likewise
        // so if i want a triangle i would print it here 
    }
     console.log(empty)
     // This prints all in a same line with same columns
    // Printed if I want output in the box format

}